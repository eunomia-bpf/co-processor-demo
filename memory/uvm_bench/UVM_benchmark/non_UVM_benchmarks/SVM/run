#!/bin/bash
# SVM requires data files in ../../data/SVM/
if [ -f ../../data/SVM/training_data.txt ]; then
    ./svm ../../data/SVM/training_data.txt
else
    echo "SVM data not found. Please check ../../data/SVM/"
    exit 1
fi
