#!/bin/bash
# SVM requires 3 arguments: C (int), M (int), ACCURACY (float)
# Default values based on typical SVM training
C=${1:-100}
M=${2:-1000}
ACCURACY=${3:-0.001}

echo "Running SVM with C=$C, M=$M, ACCURACY=$ACCURACY"
./svm_uvm $C $M $ACCURACY
